[build-system]
requires = ["setuptools>=40.8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mahjong-agent"
version = "0.1.0"
description = "Botzone-oriented Chinese Standard Mahjong agent"
requires-python = ">=3.6"

[tool.pytest.ini_options]
testpaths = ["tests"]
